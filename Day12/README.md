This lab analyzes the layer 2 and layer 3 encapsulation process by requiring you to track how source and destination MAC addresses change at every network hop while Layer 3 IP addresses remain constant.







You will use Cisco Packet Tracer's Simulation Mode and CLI commands to verify MAC address tables, ARP tables, and the exact hardware address transitions as packets cross multiple router segments.
